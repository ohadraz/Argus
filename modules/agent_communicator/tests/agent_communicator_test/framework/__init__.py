"""Test support shared across `agent_communicator`'s own suites.

Split by what a helper is: `builders` makes the events an incident produced,
`assertions` says what the relay had to report, and `slack` reads back what the
double actually holds.
"""
