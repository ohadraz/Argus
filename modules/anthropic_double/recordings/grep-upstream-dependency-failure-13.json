{
  "container": null,
  "content": [
    {
      "signature": "",
      "thinking": "The real fault is that a third-party payment provider's 503 error takes down the entire account page, since the card is fetched inline with no fallback. The fix should be to degrade gracefully\u2014render the page without the card and surface the provider failure alongside it, similar to how cache_failure is handled, instead of failing the whole request.\n\nI notice the module docstring deliberately insists on no fallback, and the existing test asserts the page should fail on provider timeout\u2014that test embodies the fault and needs updating too. The pattern here mirrors the summary_cache case: a non-essential dependency's failure should be reported beside the response, not fail the whole thing, so this fix belongs in account_page.\n\nI'll catch PaymentProviderFailed around the card lookup, add a `card_failure` field to the rendered page, and render with `card_last_four=None` on failure rather than raising. I won't try to fix the latency/timeout issue since there's no seam for it\u2014keeping this a minimal, contained change: import the exception type, add the new field, wrap just the card retrieval in its own try/except inside the existing try block, and leave the visit-recording logic untouched since the success path is unaffected. Then I need to update the test that currently expects the page to fail on provider timeout, since that behavior is exactly what we're changing.\n\nFor the format of `card_failure`, I want to mirror how `cache_failure` and the diagnostic log line are built\u2014using the exception type name, message, and raise location\u2014so the degraded page still gives enough context to debug provider issues, rather than just a bare string.\n\nI'm also weighing whether to catch broader exceptions around the card fetch versus only `PaymentProviderFailed`. Since the outer boundary already handles unexpected exceptions by failing the whole page, I decide to only catch `PaymentProviderFailed` specifically for card degradation, leaving any other transport errors to be wrapped upstream by the caller's seam rather than swallowed here.\n\nActually the failure isn't silenced since it's still reported in card_failure with the chained original message. I'll keep the fix minimal\u2014only catching PaymentProviderFailed in account_page\u2014since the incident's 503 was returned as a status by the seam anyway, which matches the evidence cleanly. Time to write the files.\n\n",
      "type": "thinking"
    },
    {
      "text": "I have the fault: the account page treats a third-party card lookup as a hard requirement, so a provider 503 fails the whole page. Let me write the fix.",
      "type": "text"
    },
    {
      "caller": {
        "type": "direct"
      },
      "id": "toolu_01P43orFRh9YYTzL2btCQLsU",
      "input": {
        "explanation": "Fault: `serve_account_page` fetches the stored card from the payment provider inside the same try block that guards the whole render, so any `PaymentProviderFailed` - including the provider answering 503 - aborts the entire page. Everything else on the page (the spend figure, which Io works out for itself, and the monthly statement) is already computed and correct at that point, yet it is thrown away and the request is reported as an Io error. That is why a provider outage at 16:06Z turned into a 100% error rate on io-shop's own account page: a display-only panel belonging to another company sat on the request's success path.\n\nThe shop already has the right shape for this one dependency further down: `summary_cache` raises `CacheUnreachable`, the page catches it, renders anyway, and reports the words beside the failure in `cache_failure`. The card is the same kind of thing - one panel the page shows, sourced from somebody else - and it is now treated the same way.\n\nFix: the card lookup moves into `_the_card_for`, which catches `PaymentProviderFailed` (and only that - anything unexpected still reaches the boundary and still fails the request) and returns no card plus the failure's own words. `RenderedPage` gains `card_failure`, formatted exactly as `failure` is, so the log line an investigation reads is unchanged: type, host, status, and `src/io_shop/payment_provider.py:<line>`. The page renders with `card_last_four=None`, `failure=None`, and `card_failure` set - a page that succeeded while something underneath it was broken, visible in the logs without being an outage. Nothing is retried, nothing is invented, and the provider's failure is not silenced; it is just no longer the shop's own error.\n\nTests: the two provider cases in `tests/io_shop/test_account_page.py` previously asserted the old behaviour (a down provider fails the page) - they now assert the page renders with the figure, no card, and a `card_failure` naming the host, status and raising line, which is the case that was failing during the incident. A case is added for the provider recovering (a card, no `card_failure`) so both states are pinned, and `tests/io_shop/test_visits.py` gains a case showing a degraded page still records the shopper's visit with the figure they were shown rather than an error string.</explanation>\n<parameter name=\"files\">[{\"path\": \"src/io_shop/account_page.py\", \"content\": \"from __future__ import annotations\\n\\nimport traceback\\nfrom dataclasses import dataclass\\nfrom pathlib import PurePath\\n\\nfrom io_shop.accounts import Account\\nfrom io_shop.monthly_statement import (\\n    MonthlyStatement,\\n    StatementPeriod,\\n    render_monthly_statement,\\n)\\nfrom io_shop.payment_provider import (\\n    AskTheProvider,\\n    PaymentProviderFailed,\\n    StoredCard,\\n    card_on_file,\\n)\\nfrom io_shop.spend_summary import render_spend_summary\\nfrom io_shop.summary_cache import (\\n    CacheEndpoint,\\n    CacheUnreachable,\\n    LookUpSummary,\\n    cached_summary,\\n)\\nfrom io_shop.visits import record_visit\\n\\n\\\"\\\"\\\"Serving one account page - the shop's request boundary.\\n\\nThis is the line at which an exception stops being a bug and becomes an\\nincident: everything below it raises, and here it is caught, recorded in the\\nshop's own words, and turned into a failed response. It is the only place in the\\nshop that catches broadly, and it does so on purpose - a request handler that let\\nan unexpected error escape would take the worker with it instead of reporting a\\nrate somebody can alert on.\\n\\nWhat reaches that line is the shop's own failures. The two things the page gets\\nfrom somewhere else - the cached figure and the card on file - are asked for in\\nways that can be answered with \\\"not today\\\": the page renders what it has and\\nsays, beside the render, what it could not reach. An outage at another company\\nis then a missing panel here rather than an error rate here.\\n\\\"\\\"\\\"\\n\\n\\n@dataclass(frozen=True)\\nclass RenderedPage:\\n    \\\"\\\"\\\"How serving one account page went.\\n\\n    Either the page rendered, in which case the figure it shows is here, or it\\n    failed, in which case nothing is and `failure` carries the error's own words\\n    and the line they were raised on - because those are what reach the log and\\n    what a reader diagnoses from. A generic \\\"request failed\\\" would describe\\n    every incident equally, and the error's words alone name a fault without\\n    naming where it lives.\\n\\n    `statement` is the monthly statement panel, on the pages the newest rollout\\n    reached and absent everywhere else. It sits beside the figure rather than\\n    replacing it: the statement is a second thing the page shows, so a request\\n    outside the rollout renders the page it always rendered rather than a\\n    shorter one.\\n\\n    `served_from_cache` says whether the figure was read rather than worked out.\\n    It is reported on a page that rendered perfectly well, because it is the\\n    difference between the two ways of rendering perfectly well - and the share\\n    of requests taking each is what says whether the shop's fast path is there.\\n\\n    `cache_failure` carries the words of a cache that could not be reached. It\\n    sits beside `failure` rather than in it, and the distinction is the whole\\n    scenario: this is a page that *succeeded* while something underneath it was\\n    broken, so a reader sees it in the logs without seeing it in the error rate.\\n\\n    `card_failure` is the same arrangement for the payment provider, and for the\\n    same reason. The card is one panel sourced from another company; when that\\n    company will not answer, `card_last_four` is absent and these are the words\\n    that say why. The rest of the page is Io's own work and is still correct, so\\n    a provider outage is a page missing its card rather than a page nobody got.\\n    \\\"\\\"\\\"\\n\\n    figure_cents: int | None\\n    card_last_four: str | None\\n    failure: str | None\\n    served_from_cache: bool = False\\n    cache_failure: str | None = None\\n    statement: MonthlyStatement | None = None\\n    card_failure: str | None = None\\n\\n\\ndef serve_account_page(account: Account,\\n                       use_monthly_summary: bool,\\n                       ask_the_provider: AskTheProvider,\\n                       look_up_summary: LookUpSummary | None = None,\\n                       cache_endpoint: CacheEndpoint | None = None,\\n                       use_typical_spend: bool = False,\\n                       use_monthly_statement: bool = False,\\n                       statement_period: StatementPeriod | None = None\\n                       ) -> RenderedPage:\\n    \\\"\\\"\\\"Renders the account page, reporting a failure rather than raising one.\\n\\n    Two things are shown: what the shopper averages per item, which Io works out\\n    for itself, and the card it would charge, which only the payment provider\\n    knows. The second is a call to another company from inside a page render,\\n    which is ordinary - and is why it is allowed to come back empty. A provider\\n    that will not answer costs the page its card panel and nothing else.\\n\\n    `use_monthly_summary` and `use_typical_spend` are the rollout decisions\\n    already made - whether this request is one of the ones each new figure is\\n    live for. The page does not make those decisions itself; it is told, the way\\n    a handler is told by the flag SDK that evaluated for this user. Two of them\\n    because there are two rollouts running, and a page carrying one boolean for\\n    both would be a page that cannot say which feature a request got.\\n\\n    `look_up_summary` and `cache_endpoint` are how the figure is looked for\\n    before it is worked out. Both optional and both absent together, because a\\n    deployment that configured no cache has none - and a page that insisted on\\n    one would make an optimisation into a requirement, which is the very thing\\n    this shop's cache is not.\\n\\n    `use_monthly_statement` is the third rollout decision, and it arrives here\\n    already made like the other two. `statement_period` is which month the\\n    request is asking about, which the page is told rather than deriving: the\\n    shop's purchase records carry a month flag and no date, so the only thing\\n    here that knows the calendar is whoever handled the request.\\n    \\\"\\\"\\\"\\n    try:\\n        figure_cents, from_cache, cache_failure = _the_figure_for(\\n            account, use_monthly_summary, look_up_summary, cache_endpoint,\\n            use_typical_spend\\n        )\\n        statement = _the_statement_for(\\n            account, use_monthly_statement, statement_period\\n        )\\n        card, card_failure = _the_card_for(account.shopper_id, ask_the_provider)\\n    except Exception as error:  # noqa: BLE001 - the boundary records anything\\n        failure = f\\\"{type(error).__name__}: {error} at {_where_it_was_raised(error)}\\\"\\n        record_visit(account.shopper_id, failure)\\n\\n        return RenderedPage(figure_cents=None, card_last_four=None, failure=failure)\\n\\n    record_visit(account.shopper_id, str(figure_cents))\\n\\n    return RenderedPage(figure_cents=figure_cents,\\n                        card_last_four=card.last_four if card is not None else None,\\n                        failure=None,\\n                        served_from_cache=from_cache,\\n                        cache_failure=cache_failure,\\n                        statement=statement,\\n                        card_failure=card_failure)\\n\\n\\ndef _the_card_for(shopper_id: str,\\n                  ask_the_provider: AskTheProvider\\n                  ) -> tuple[StoredCard | None, str | None]:\\n    \\\"\\\"\\\"The card the provider holds, and the words of a provider that would not\\n    give one.\\n\\n    Returned rather than raised onward, because a provider that is down is not\\n    this request's failure. The figure on the page is Io's own work and is as\\n    correct as it ever was, so the page renders with one panel short - which is\\n    what a shop can actually do about somebody else's outage. Failing every\\n    account page instead would turn their incident into Io's, and give a reader\\n    an error rate to explain rather than a dependency to name.\\n\\n    Only `PaymentProviderFailed` is caught: that is the provider answering with\\n    something other than a card, which is exactly the fact this degrades on.\\n    Anything else the seam raises is unexpected and still reaches the boundary,\\n    where it is reported as a failure like any other.\\n    \\\"\\\"\\\"\\n    try:\\n        return card_on_file(shopper_id, ask_the_provider), None\\n    except PaymentProviderFailed as unavailable:\\n        return None, (\\n            f\\\"{type(unavailable).__name__}: {unavailable} \\\"\\n            f\\\"at {_where_it_was_raised(unavailable)}\\\"\\n        )\\n\\n\\ndef _the_statement_for(account: Account,\\n                       use_monthly_statement: bool,\\n                       period: StatementPeriod | None) -> MonthlyStatement | None:\\n    \\\"\\\"\\\"The monthly statement panel, where this request is one the rollout\\n    reached, and nothing where it is not.\\n\\n    Nothing also where the request arrived without a period. That is a\\n    misconfigured caller rather than a shopper's month, and rendering a\\n    statement titled with a month nobody named would put a wrong heading over\\n    correct figures - see `io_shop.monthly_statement.StatementPeriod`, which is\\n    where that argument is made at length. A missing panel is visible to\\n    whoever configured the rollout; a wrongly titled one is visible only to the\\n    shopper.\\n    \\\"\\\"\\\"\\n    if not use_monthly_statement or period is None:\\n        return None\\n\\n    return render_monthly_statement(account, period)\\n\\n\\ndef _the_figure_for(\\n    account: Account,\\n    use_monthly_summary: bool,\\n    look_up_summary: LookUpSummary | None,\\n    cache_endpoint: CacheEndpoint | None,\\n    use_typical_spend: bool\\n) -> tuple[int, bool, str | None]:\\n    \\\"\\\"\\\"The figure to show, whether it came from the cache, and what the cache\\n    said if it could not be reached.\\n\\n    The cache is asked first and is allowed to fail. Whatever it does - answers\\n    with a figure, answers with nothing, or cannot be reached at all - this\\n    returns a figure, because the shop can always work one out. That is the\\n    fallback the whole scenario rests on: the page is correct either way, and\\n    the only thing the cache decides is how long getting here took.\\n\\n    A cache failure is returned rather than raised onward, because it is not\\n    this request's failure. The request succeeded.\\n    \\\"\\\"\\\"\\n    if look_up_summary is None or cache_endpoint is None:\\n        return render_spend_summary(\\n            account,\\n            use_monthly_summary=use_monthly_summary,\\n            use_typical_spend=use_typical_spend\\n        ), False, None\\n\\n    cache_failure: str | None = None\\n\\n    try:\\n        found = cached_summary(account.shopper_id, look_up_summary, cache_endpoint)\\n    except CacheUnreachable as unreachable:\\n        found, cache_failure = None, str(unreachable)\\n\\n    if found is not None:\\n        return found, True, cache_failure\\n\\n    return render_spend_summary(\\n        account,\\n        use_monthly_summary=use_monthly_summary,\\n        use_typical_spend=use_typical_spend\\n    ), False, cache_failure\\n\\n\\ndef _where_it_was_raised(error: BaseException) -> str:\\n    \\\"\\\"\\\"The innermost frame, as `path/to/file.py:line`.\\n\\n    The innermost rather than the boundary's own, because the boundary is where\\n    every failure is caught and is therefore the same answer for all of them.\\n    What a reader wants is the line that actually divided by zero.\\n\\n    Said as a repository path rather than the absolute one the interpreter\\n    reports, so that the log names a file somebody can open. The frame's path is\\n    wherever this happens to be installed - a container's site-packages, an\\n    editable checkout - and none of those spellings exist in the repository.\\n    \\\"\\\"\\\"\\n    frames = traceback.extract_tb(error.__traceback__)\\n\\n    if not frames:\\n        return \\\"an unknown line\\\"\\n\\n    innermost = frames[-1]\\n\\n    return f\\\"{_as_a_repository_path(innermost.filename)}:{innermost.lineno}\\\"\\n\\n\\ndef _as_a_repository_path(filename: str) -> str:\\n    \\\"\\\"\\\"The file as the repository spells it, if it can be recognised.\\n\\n    Cut at the package directory and put back under `src/`, which is where this\\n    shop keeps its source. Anything unrecognisable is reported whole rather than\\n    guessed at - a wrong path in a log is worse than a long one, because it\\n    sends a reader to a file that does not exist and looks authoritative doing\\n    it.\\n    \\\"\\\"\\\"\\n    parts = PurePath(filename).parts\\n\\n    if _PACKAGE not in parts:\\n        return filename\\n\\n    return \\\"/\\\".join((\\\"src\\\", *parts[parts.index(_PACKAGE):]))\\n\\n\\n_PACKAGE = \\\"io_shop\\\"\\n\"}, {\"path\": \"src/io_shop/payment_provider.py\", \"content\": \"from __future__ import annotations\\n\\nfrom collections.abc import Callable\\nfrom dataclasses import dataclass\\n\\n\\\"\\\"\\\"The card Io keeps on file for a shopper, which Io does not keep.\\n\\nThe account page shows the card a shopper will be charged with, and the shop has\\nnever held that number: it lives with the payment provider, and the page asks\\nfor it while it renders. That makes the provider part of every account page -\\nanother company's service on Io's request path, which is the ordinary shape of a\\nshop and the reason an outage somewhere else can become an outage here.\\n\\nNothing in this module retries or invents a card. When the provider will not\\nanswer, that is said plainly - in words naming the host, the path and the status\\n- and what the shop does about it is the page's decision, not this module's. The\\naccount page degrades: it renders without the card panel and reports these words\\nbeside the render. Card details are not worth failing a page for, and a shop\\nthat fails every account page because somebody else is down has turned their\\nincident into its own.\\n\\nHow the provider is reached is the caller's to supply. The shop states what it\\nasks for and what it does with the answer, and whoever is running it says where\\nthat answer comes from.\\n\\\"\\\"\\\"\\n\\n# Who Io banks with. A host rather than a product name, because a host is what\\n# appears in a failed request's own words - and those words are what somebody\\n# reading the logs at three in the morning has to recognise as not-ours.\\nPROVIDER_HOST = \\\"api.io-pay.example\\\"\\n\\n# What is asked for. Spelled out rather than built, so the path in a failure\\n# line is the path the shop actually requested.\\nCARD_PATH = \\\"/v1/shoppers/{shopper_id}/card\\\"\\n\\n# The only answer that carries a card.\\n_OK = 200\\n\\n\\n@dataclass(frozen=True)\\nclass StoredCard:\\n    \\\"\\\"\\\"A card as the provider describes it: enough to show, never enough to\\n    charge with.\\n\\n    The provider holds the number; what it hands back is what a page may\\n    display. A shop that received more than this would be storing card details\\n    it went to a provider precisely to avoid storing.\\n    \\\"\\\"\\\"\\n\\n    brand: str\\n    last_four: str\\n\\n\\n@dataclass(frozen=True)\\nclass ProviderAnswer:\\n    \\\"\\\"\\\"One reply from the provider: the status it answered with, and the card if\\n    it had one.\\n\\n    The status is carried rather than interpreted by whoever fetched it, because\\n    turning a provider's status into the shop's own failure is this module's\\n    job - and a fetcher that raised on its own would put that decision in as\\n    many places as there are ways to reach the provider.\\n    \\\"\\\"\\\"\\n\\n    status: int\\n    card: StoredCard | None = None\\n\\n\\nclass PaymentProviderFailed(Exception):\\n    \\\"\\\"\\\"The provider did not answer with a card.\\n\\n    Named for what the shop can see. Io has no way to tell a provider that is\\n    down from one that is refusing this particular shopper, and inventing the\\n    distinction in an exception type would be the shop claiming knowledge of\\n    another company's internals.\\n\\n    Raised so that the caller has to decide what to do about it, and caught by\\n    the account page, which renders without the card. The raise is not\\n    pointless: it is how the page learns there is no card *and* what to say\\n    about it, which a function returning `None` could not tell it.\\n    \\\"\\\"\\\"\\n\\n\\n# How the provider is reached, given a shopper. A seam rather than a request,\\n# because the shop is rendered many times over to produce a minute of telemetry\\n# and a socket per render would be thousands of them per read.\\ntype AskTheProvider = Callable[[str], ProviderAnswer]\\n\\n\\ndef card_on_file(shopper_id: str, ask: AskTheProvider) -> StoredCard:\\n    \\\"\\\"\\\"The card the provider holds for this shopper.\\n\\n    Raises `PaymentProviderFailed` for anything that is not a card, and the\\n    message is the point of the function: it names the provider's host, the\\n    path Io asked for, and the status that came back. A failure that said only\\n    \\\"could not load card\\\" would be indistinguishable, in a log, from a bug in\\n    the shop.\\n    \\\"\\\"\\\"\\n    answer = ask(shopper_id)\\n\\n    if answer.status != _OK or answer.card is None:\\n        raise PaymentProviderFailed(\\n            f\\\"{PROVIDER_HOST} returned {answer.status} for \\\"\\n            f\\\"{CARD_PATH.format(shopper_id=shopper_id)}\\\"\\n        )\\n\\n    return answer.card\\n\"}, {\"path\": \"tests/io_shop/test_account_page.py\", \"content\": \"from __future__ import annotations\\n\\nfrom pathlib import Path\\n\\nfrom io_shop import payment_provider, spend_summary\\nfrom io_shop.account_page import serve_account_page\\nfrom io_shop.accounts import Account, Purchase\\nfrom io_shop.payment_provider import AskTheProvider, ProviderAnswer, StoredCard\\nfrom io_shop.summary_cache import CacheAnswer, CacheEndpoint, LookUpSummary\\n\\n\\\"\\\"\\\"The shop's request boundary: what a caller sees when the page fails.\\n\\nThree things worth pinning: a failure is reported rather than raised - a handler\\nthat let it escape would take the worker down - the report keeps the error's own\\nwords, which are what a reader diagnoses from, and a payment provider that will\\nnot answer costs the page its card panel in words that name the provider,\\nwithout costing the shopper the page.\\n\\\"\\\"\\\"\\n\\n\\ndef a_provider_holding_a_card() -> AskTheProvider:\\n    return lambda dont_care_shopper: ProviderAnswer(\\n        status=200, card=StoredCard(brand=\\\"visa\\\", last_four=\\\"4242\\\")\\n    )\\n\\n\\ndef a_provider_that_is_down() -> AskTheProvider:\\n    return lambda dont_care_shopper: ProviderAnswer(status=503)\\n\\n\\ndef an_account_idle_this_month(*prices: int) -> Account:\\n    return Account(\\n        shopper_id=\\\"shopper-idle-this-month\\\",\\n        purchases=tuple(\\n            Purchase(price_cents=price, in_current_month=False) for price in prices\\n        ),\\n        total_cents=sum(prices),\\n        total_this_month_cents=0,\\n    )\\n\\n\\ndef an_account_that_never_bought_anything() -> Account:\\n    \\\"\\\"\\\"A shopper with no purchase history at all - the lifetime figure's own\\n    empty divisor, and the failure this file is built on.\\n    \\\"\\\"\\\"\\n    return Account(\\n        shopper_id=\\\"shopper-with-no-history\\\", purchases=(), total_cents=0,\\n        total_this_month_cents=0\\n    )\\n\\n\\ndef test_a_page_that_renders_carries_the_figure_and_no_failure() -> None:\\n    account = an_account_idle_this_month(1000, 3000)\\n\\n    page = serve_account_page(account,\\n                              use_monthly_summary=False,\\n                              ask_the_provider=a_provider_holding_a_card())\\n\\n    assert page.figure_cents == 2000\\n    assert page.card_last_four == \\\"4242\\\"\\n    assert page.failure is None\\n\\n\\ndef test_a_page_that_breaks_is_reported_rather_than_raised() -> None:\\n    page = serve_account_page(\\n        an_account_that_never_bought_anything(),\\n        use_monthly_summary=False,\\n        ask_the_provider=a_provider_holding_a_card()\\n    )\\n\\n    assert page.figure_cents is None\\n    assert page.failure is not None\\n\\n\\ndef test_a_failure_keeps_the_errors_own_words() -> None:\\n    # \\\"request failed\\\" describes every incident equally. The type and message\\n    # are what put `ZeroDivisionError` in the logs an investigation reads.\\n    page = serve_account_page(\\n        an_account_that_never_bought_anything(),\\n        use_monthly_summary=False,\\n        ask_the_provider=a_provider_holding_a_card()\\n    )\\n\\n    assert page.failure is not None\\n    assert page.failure.startswith(\\\"ZeroDivisionError: division by zero\\\")\\n\\n\\ndef test_a_failure_names_the_line_it_was_raised_on() -> None:\\n    # The innermost frame, not the boundary's own: every failure in the shop is\\n    # caught in the same place, so the boundary's line describes all of them and\\n    # locates none. This is the one that divided by zero.\\n    page = serve_account_page(\\n        an_account_that_never_bought_anything(),\\n        use_monthly_summary=False,\\n        ask_the_provider=a_provider_holding_a_card()\\n    )\\n\\n    assert page.failure is not None\\n    assert \\\"at src/io_shop/spend_summary.py:\\\" in page.failure\\n\\n\\ndef test_the_line_a_failure_names_is_the_one_that_raised_it() -> None:\\n    # The number, not just the file. A frame reported off by a few lines sends a\\n    # reader to code that is fine and reads as authoritatively as a right one.\\n    source = (\\n        Path(spend_summary.__file__).read_text(encoding=\\\"utf-8\\\").splitlines()\\n    )\\n    page = serve_account_page(\\n        an_account_that_never_bought_anything(),\\n        use_monthly_summary=False,\\n        ask_the_provider=a_provider_holding_a_card()\\n    )\\n\\n    assert page.failure is not None\\n    named = int(page.failure.rsplit(\\\":\\\", 1)[1])\\n\\n    assert \\\"//\\\" in source[named - 1]\\n\\n\\ndef test_a_provider_that_will_not_answer_costs_the_card_and_not_the_page() -> None:\\n    # The incident, as a case. The card is one panel sourced from another\\n    # company; the figure beside it is Io's own work and is still correct. A\\n    # page that failed here would put somebody else's outage into Io's error\\n    # rate, which is what took every account page down when the provider\\n    # started answering 503.\\n    page = serve_account_page(an_account_idle_this_month(1000, 3000),\\n                              use_monthly_summary=False,\\n                              ask_the_provider=a_provider_that_is_down())\\n\\n    assert page.failure is None\\n    assert page.figure_cents == 2000\\n    assert page.card_last_four is None\\n\\n\\ndef test_a_provider_failure_is_reported_beside_the_page_not_in_it() -> None:\\n    # The host, the status and the raising line, because those are what tell a\\n    # reader at three in the morning that the fault is not in this repository -\\n    # and they belong in `card_failure`, where they are visible in the logs\\n    # without being visible in the error rate.\\n    page = serve_account_page(an_account_idle_this_month(1000, 3000),\\n                              use_monthly_summary=False,\\n                              ask_the_provider=a_provider_that_is_down())\\n\\n    assert page.failure is None\\n    assert page.card_failure is not None\\n    assert page.card_failure.startswith(\\\"PaymentProviderFailed: \\\")\\n    assert payment_provider.PROVIDER_HOST in page.card_failure\\n    assert \\\"503\\\" in page.card_failure\\n    assert \\\"at src/io_shop/payment_provider.py:\\\" in page.card_failure\\n\\n\\ndef test_a_provider_that_answers_reports_no_card_failure() -> None:\\n    # The other state. Degrading when the provider is down is only right if the\\n    # page is unchanged when it is up.\\n    page = serve_account_page(an_account_idle_this_month(1000, 3000),\\n                              use_monthly_summary=False,\\n                              ask_the_provider=a_provider_holding_a_card())\\n\\n    assert page.card_last_four == \\\"4242\\\"\\n    assert page.card_failure is None\\n\\n\\ndef a_cache_holding(summary_cents: int) -> LookUpSummary:\\n    return lambda dont_care_shopper: CacheAnswer(\\n        reached=True, summary_cents=summary_cents\\n    )\\n\\n\\ndef a_cache_holding_nothing() -> LookUpSummary:\\n    return lambda dont_care_shopper: CacheAnswer(reached=True)\\n\\n\\ndef a_cache_that_cannot_be_reached() -> LookUpSummary:\\n    return lambda dont_care_shopper: CacheAnswer(reached=False)\\n\\n\\nSOME_CACHE_ENDPOINT = CacheEndpoint(host=\\\"cache.io-shop.svc.cluster.local\\\", port=6379)\\n\\n\\ndef test_a_page_whose_figure_was_cached_says_so() -> None:\\n    page = serve_account_page(an_account_idle_this_month(1000, 3000),\\n                              use_monthly_summary=False,\\n                              ask_the_provider=a_provider_holding_a_card(),\\n                              look_up_summary=a_cache_holding(999),\\n                              cache_endpoint=SOME_CACHE_ENDPOINT)\\n\\n    assert page.figure_cents == 999\\n    assert page.served_from_cache\\n\\n\\ndef test_a_miss_is_computed_and_the_page_is_still_correct() -> None:\\n    account = an_account_idle_this_month(1000, 3000)\\n\\n    computed = serve_account_page(account,\\n                                  use_monthly_summary=False,\\n                                  ask_the_provider=a_provider_holding_a_card(),\\n                                  look_up_summary=a_cache_holding_nothing(),\\n                                  cache_endpoint=SOME_CACHE_ENDPOINT)\\n    without_a_cache_at_all = serve_account_page(\\n        account,\\n        use_monthly_summary=False,\\n        ask_the_provider=a_provider_holding_a_card()\\n    )\\n\\n    assert computed.figure_cents == without_a_cache_at_all.figure_cents\\n    assert not computed.served_from_cache\\n\\n\\ndef test_a_cache_nobody_can_reach_does_not_fail_the_page() -> None:\\n    # The whole scenario rests on this. The fallback is the designed behaviour,\\n    # so losing the cache is a slowdown and not an outage - which is exactly\\n    # why nobody notices it in the error rate.\\n    account = an_account_idle_this_month(1000, 3000)\\n\\n    page = serve_account_page(account,\\n                              use_monthly_summary=False,\\n                              ask_the_provider=a_provider_holding_a_card(),\\n                              look_up_summary=a_cache_that_cannot_be_reached(),\\n                              cache_endpoint=SOME_CACHE_ENDPOINT)\\n\\n    assert page.failure is None\\n    assert page.figure_cents is not None\\n    assert not page.served_from_cache\\n\\n\\ndef test_an_unreachable_cache_is_reported_beside_the_failure_not_in_it() -> None:\\n    # A page that succeeded while something underneath it was broken. The\\n    # distinction is the incident: a reader sees this in the logs without\\n    # seeing it in the error rate.\\n    page = serve_account_page(an_account_idle_this_month(1000, 3000),\\n                              use_monthly_summary=False,\\n                              ask_the_provider=a_provider_holding_a_card(),\\n                              look_up_summary=a_cache_that_cannot_be_reached(),\\n                              cache_endpoint=SOME_CACHE_ENDPOINT)\\n\\n    assert page.failure is None\\n    assert page.cache_failure is not None\\n    assert \\\"6379\\\" in page.cache_failure\\n\"}, {\"path\": \"tests/io_shop/test_visits.py\", \"content\": \"from __future__ import annotations\\n\\nimport pytest\\n\\nfrom io_shop.account_page import serve_account_page\\nfrom io_shop.accounts import Account, Purchase\\nfrom io_shop.payment_provider import AskTheProvider, ProviderAnswer, StoredCard\\nfrom io_shop.visits import (\\n    forget_every_visit,\\n    how_many_shoppers_are_remembered,\\n    record_visit,\\n    what_they_saw_last_time,\\n)\\n\\n\\\"\\\"\\\"What the account page keeps about the shoppers who have been by.\\n\\nThe store itself is small and does what it says. What these pin down is the\\npart that matters to an incident: it grows with every new shopper and shrinks\\nfor no reason at all, which is the shape of the fault the leak scenario is\\nabout - and that a new process starts empty, which is why restarting the shop\\nis worth anything.\\n\\\"\\\"\\\"\\n\\n\\n@pytest.fixture(autouse=True)\\ndef a_shop_that_has_just_started() -> None:\\n    \\\"\\\"\\\"Every case begins with an empty store.\\n\\n    The store is module state, so without this each test would inherit\\n    whatever the last one left - which is the bug under discussion, and a poor\\n    thing to also have in the tests about it.\\n    \\\"\\\"\\\"\\n    forget_every_visit()\\n\\n\\ndef a_provider_holding_a_card() -> AskTheProvider:\\n    \\\"\\\"\\\"The provider answering, which is what it does in almost every case here -\\n    these are about what the page remembers, not about who it asks.\\\"\\\"\\\"\\n    return lambda dont_care_shopper: ProviderAnswer(\\n        status=200, card=StoredCard(brand=\\\"visa\\\", last_four=\\\"4242\\\")\\n    )\\n\\n\\ndef a_provider_that_is_down() -> AskTheProvider:\\n    return lambda dont_care_shopper: ProviderAnswer(status=503)\\n\\n\\ndef an_account(shopper_id: str, *prices: int) -> Account:\\n    return Account(\\n        shopper_id=shopper_id,\\n        purchases=tuple(\\n            Purchase(price_cents=price, in_current_month=False) for price in prices\\n        ),\\n        total_cents=sum(prices),\\n        total_this_month_cents=0,\\n    )\\n\\n\\ndef test_a_shopper_who_has_been_here_before_is_remembered() -> None:\\n    record_visit(\\\"shopper-1\\\", \\\"2000\\\")\\n\\n    assert what_they_saw_last_time(\\\"shopper-1\\\") == \\\"2000\\\"\\n\\n\\ndef test_a_shopper_who_has_never_been_here_is_not() -> None:\\n    assert what_they_saw_last_time(\\\"shopper-nobody\\\") is None\\n\\n\\ndef test_the_newest_visit_is_the_one_kept() -> None:\\n    record_visit(\\\"shopper-1\\\", \\\"2000\\\")\\n    record_visit(\\\"shopper-1\\\", \\\"3000\\\")\\n\\n    assert what_they_saw_last_time(\\\"shopper-1\\\") == \\\"3000\\\"\\n\\n\\ndef test_every_new_shopper_adds_to_what_the_process_is_holding() -> None:\\n    # The fault, stated plainly: what this holds is a function of how many\\n    # different shoppers have been by, and nothing takes any of it away.\\n    for shopper in range(50):\\n        record_visit(f\\\"shopper-{shopper}\\\", \\\"2000\\\")\\n\\n    assert how_many_shoppers_are_remembered() == 50\\n\\n\\ndef test_a_shopper_coming_back_adds_nothing() -> None:\\n    # Worth pinning because it is what makes the climb track *traffic* rather\\n    # than requests: a shop serving the same hundred shoppers all day holds a\\n    # hundred entries, and one serving the internet holds the internet.\\n    for _ in range(50):\\n        record_visit(\\\"shopper-1\\\", \\\"2000\\\")\\n\\n    assert how_many_shoppers_are_remembered() == 1\\n\\n\\ndef test_serving_a_page_records_the_visit() -> None:\\n    serve_account_page(an_account(\\\"shopper-1\\\", 1000, 3000),\\n                       use_monthly_summary=False,\\n                       ask_the_provider=a_provider_holding_a_card())\\n\\n    assert what_they_saw_last_time(\\\"shopper-1\\\") == \\\"2000\\\"\\n\\n\\ndef test_a_page_missing_its_card_records_the_figure_it_showed() -> None:\\n    # A provider outage leaves a page that rendered, so what the shopper was\\n    # shown is the figure - not an error string they never saw.\\n    serve_account_page(an_account(\\\"shopper-1\\\", 1000, 3000),\\n                       use_monthly_summary=False,\\n                       ask_the_provider=a_provider_that_is_down())\\n\\n    assert what_they_saw_last_time(\\\"shopper-1\\\") == \\\"2000\\\"\\n\\n\\ndef test_a_page_that_failed_is_a_visit_too() -> None:\\n    # The shopper was here. A store that only grew on success would leave the\\n    # shop leaking at a rate that depended on how well it was working, which is\\n    # not how retained state behaves.\\n    serve_account_page(an_account(\\\"shopper-1\\\"),\\n                       use_monthly_summary=False,\\n                       ask_the_provider=a_provider_holding_a_card())\\n\\n    remembered = what_they_saw_last_time(\\\"shopper-1\\\")\\n\\n    assert remembered is not None\\n    assert remembered.startswith(\\\"ZeroDivisionError\\\")\\n\\n\\ndef test_serving_pages_to_many_shoppers_holds_one_entry_each() -> None:\\n    for shopper in range(20):\\n        serve_account_page(\\n            an_account(f\\\"shopper-{shopper}\\\", 1000, 3000),\\n            use_monthly_summary=False,\\n            ask_the_provider=a_provider_holding_a_card()\\n        )\\n\\n    assert how_many_shoppers_are_remembered() == 20\\n\\n\\ndef test_a_restarted_shop_is_holding_nothing() -> None:\\n    # What a new process starts with, and the whole of what a restart buys. The\\n    # fault is still in the code when it comes back, which is why the climb\\n    # begins again - but it begins from here.\\n    for shopper in range(20):\\n        record_visit(f\\\"shopper-{shopper}\\\", \\\"2000\\\")\\n\\n    forget_every_visit()\\n\\n    assert how_many_shoppers_are_remembered() == 0\\n\"}]",
        "files": [
          {
            "content": "from __future__ import annotations\n\nimport traceback\nfrom dataclasses import dataclass\nfrom pathlib import PurePath\n\nfrom io_shop.accounts import Account\nfrom io_shop.monthly_statement import (\n    MonthlyStatement,\n    StatementPeriod,\n    render_monthly_statement,\n)\nfrom io_shop.payment_provider import (\n    AskTheProvider,\n    PaymentProviderFailed,\n    StoredCard,\n    card_on_file,\n)\nfrom io_shop.spend_summary import render_spend_summary\nfrom io_shop.summary_cache import (\n    CacheEndpoint,\n    CacheUnreachable,\n    LookUpSummary,\n    cached_summary,\n)\nfrom io_shop.visits import record_visit\n\n\"\"\"Serving one account page - the shop's request boundary.\n\nThis is the line at which an exception stops being a bug and becomes an\nincident: everything below it raises, and here it is caught, recorded in the\nshop's own words, and turned into a failed response. It is the only place in the\nshop that catches broadly, and it does so on purpose - a request handler that let\nan unexpected error escape would take the worker with it instead of reporting a\nrate somebody can alert on.\n\nWhat reaches that line is the shop's own failures. The two things the page gets\nfrom somewhere else - the cached figure and the card on file - are asked for in\nways that can be answered with \"not today\": the page renders what it has and\nsays, beside the render, what it could not reach. An outage at another company\nis then a missing panel here rather than an error rate here.\n\"\"\"\n\n\n@dataclass(frozen=True)\nclass RenderedPage:\n    \"\"\"How serving one account page went.\n\n    Either the page rendered, in which case the figure it shows is here, or it\n    failed, in which case nothing is and `failure` carries the error's own words\n    and the line they were raised on - because those are what reach the log and\n    what a reader diagnoses from. A generic \"request failed\" would describe\n    every incident equally, and the error's words alone name a fault without\n    naming where it lives.\n\n    `statement` is the monthly statement panel, on the pages the newest rollout\n    reached and absent everywhere else. It sits beside the figure rather than\n    replacing it: the statement is a second thing the page shows, so a request\n    outside the rollout renders the page it always rendered rather than a\n    shorter one.\n\n    `served_from_cache` says whether the figure was read rather than worked out.\n    It is reported on a page that rendered perfectly well, because it is the\n    difference between the two ways of rendering perfectly well - and the share\n    of requests taking each is what says whether the shop's fast path is there.\n\n    `cache_failure` carries the words of a cache that could not be reached. It\n    sits beside `failure` rather than in it, and the distinction is the whole\n    scenario: this is a page that *succeeded* while something underneath it was\n    broken, so a reader sees it in the logs without seeing it in the error rate.\n\n    `card_failure` is the same arrangement for the payment provider, and for the\n    same reason. The card is one panel sourced from another company; when that\n    company will not answer, `card_last_four` is absent and these are the words\n    that say why. The rest of the page is Io's own work and is still correct, so\n    a provider outage is a page missing its card rather than a page nobody got.\n    \"\"\"\n\n    figure_cents: int | None\n    card_last_four: str | None\n    failure: str | None\n    served_from_cache: bool = False\n    cache_failure: str | None = None\n    statement: MonthlyStatement | None = None\n    card_failure: str | None = None\n\n\ndef serve_account_page(account: Account,\n                       use_monthly_summary: bool,\n                       ask_the_provider: AskTheProvider,\n                       look_up_summary: LookUpSummary | None = None,\n                       cache_endpoint: CacheEndpoint | None = None,\n                       use_typical_spend: bool = False,\n                       use_monthly_statement: bool = False,\n                       statement_period: StatementPeriod | None = None\n                       ) -> RenderedPage:\n    \"\"\"Renders the account page, reporting a failure rather than raising one.\n\n    Two things are shown: what the shopper averages per item, which Io works out\n    for itself, and the card it would charge, which only the payment provider\n    knows. The second is a call to another company from inside a page render,\n    which is ordinary - and is why it is allowed to come back empty. A provider\n    that will not answer costs the page its card panel and nothing else.\n\n    `use_monthly_summary` and `use_typical_spend` are the rollout decisions\n    already made - whether this request is one of the ones each new figure is\n    live for. The page does not make those decisions itself; it is told, the way\n    a handler is told by the flag SDK that evaluated for this user. Two of them\n    because there are two rollouts running, and a page carrying one boolean for\n    both would be a page that cannot say which feature a request got.\n\n    `look_up_summary` and `cache_endpoint` are how the figure is looked for\n    before it is worked out. Both optional and both absent together, because a\n    deployment that configured no cache has none - and a page that insisted on\n    one would make an optimisation into a requirement, which is the very thing\n    this shop's cache is not.\n\n    `use_monthly_statement` is the third rollout decision, and it arrives here\n    already made like the other two. `statement_period` is which month the\n    request is asking about, which the page is told rather than deriving: the\n    shop's purchase records carry a month flag and no date, so the only thing\n    here that knows the calendar is whoever handled the request.\n    \"\"\"\n    try:\n        figure_cents, from_cache, cache_failure = _the_figure_for(\n            account, use_monthly_summary, look_up_summary, cache_endpoint,\n            use_typical_spend\n        )\n        statement = _the_statement_for(\n            account, use_monthly_statement, statement_period\n        )\n        card, card_failure = _the_card_for(account.shopper_id, ask_the_provider)\n    except Exception as error:  # noqa: BLE001 - the boundary records anything\n        failure = f\"{type(error).__name__}: {error} at {_where_it_was_raised(error)}\"\n        record_visit(account.shopper_id, failure)\n\n        return RenderedPage(figure_cents=None, card_last_four=None, failure=failure)\n\n    record_visit(account.shopper_id, str(figure_cents))\n\n    return RenderedPage(figure_cents=figure_cents,\n                        card_last_four=card.last_four if card is not None else None,\n                        failure=None,\n                        served_from_cache=from_cache,\n                        cache_failure=cache_failure,\n                        statement=statement,\n                        card_failure=card_failure)\n\n\ndef _the_card_for(shopper_id: str,\n                  ask_the_provider: AskTheProvider\n                  ) -> tuple[StoredCard | None, str | None]:\n    \"\"\"The card the provider holds, and the words of a provider that would not\n    give one.\n\n    Returned rather than raised onward, because a provider that is down is not\n    this request's failure. The figure on the page is Io's own work and is as\n    correct as it ever was, so the page renders with one panel short - which is\n    what a shop can actually do about somebody else's outage. Failing every\n    account page instead would turn their incident into Io's, and give a reader\n    an error rate to explain rather than a dependency to name.\n\n    Only `PaymentProviderFailed` is caught: that is the provider answering with\n    something other than a card, which is exactly the fact this degrades on.\n    Anything else the seam raises is unexpected and still reaches the boundary,\n    where it is reported as a failure like any other.\n    \"\"\"\n    try:\n        return card_on_file(shopper_id, ask_the_provider), None\n    except PaymentProviderFailed as unavailable:\n        return None, (\n            f\"{type(unavailable).__name__}: {unavailable} \"\n            f\"at {_where_it_was_raised(unavailable)}\"\n        )\n\n\ndef _the_statement_for(account: Account,\n                       use_monthly_statement: bool,\n                       period: StatementPeriod | None) -> MonthlyStatement | None:\n    \"\"\"The monthly statement panel, where this request is one the rollout\n    reached, and nothing where it is not.\n\n    Nothing also where the request arrived without a period. That is a\n    misconfigured caller rather than a shopper's month, and rendering a\n    statement titled with a month nobody named would put a wrong heading over\n    correct figures - see `io_shop.monthly_statement.StatementPeriod`, which is\n    where that argument is made at length. A missing panel is visible to\n    whoever configured the rollout; a wrongly titled one is visible only to the\n    shopper.\n    \"\"\"\n    if not use_monthly_statement or period is None:\n        return None\n\n    return render_monthly_statement(account, period)\n\n\ndef _the_figure_for(\n    account: Account,\n    use_monthly_summary: bool,\n    look_up_summary: LookUpSummary | None,\n    cache_endpoint: CacheEndpoint | None,\n    use_typical_spend: bool\n) -> tuple[int, bool, str | None]:\n    \"\"\"The figure to show, whether it came from the cache, and what the cache\n    said if it could not be reached.\n\n    The cache is asked first and is allowed to fail. Whatever it does - answers\n    with a figure, answers with nothing, or cannot be reached at all - this\n    returns a figure, because the shop can always work one out. That is the\n    fallback the whole scenario rests on: the page is correct either way, and\n    the only thing the cache decides is how long getting here took.\n\n    A cache failure is returned rather than raised onward, because it is not\n    this request's failure. The request succeeded.\n    \"\"\"\n    if look_up_summary is None or cache_endpoint is None:\n        return render_spend_summary(\n            account,\n            use_monthly_summary=use_monthly_summary,\n            use_typical_spend=use_typical_spend\n        ), False, None\n\n    cache_failure: str | None = None\n\n    try:\n        found = cached_summary(account.shopper_id, look_up_summary, cache_endpoint)\n    except CacheUnreachable as unreachable:\n        found, cache_failure = None, str(unreachable)\n\n    if found is not None:\n        return found, True, cache_failure\n\n    return render_spend_summary(\n        account,\n        use_monthly_summary=use_monthly_summary,\n        use_typical_spend=use_typical_spend\n    ), False, cache_failure\n\n\ndef _where_it_was_raised(error: BaseException) -> str:\n    \"\"\"The innermost frame, as `path/to/file.py:line`.\n\n    The innermost rather than the boundary's own, because the boundary is where\n    every failure is caught and is therefore the same answer for all of them.\n    What a reader wants is the line that actually divided by zero.\n\n    Said as a repository path rather than the absolute one the interpreter\n    reports, so that the log names a file somebody can open. The frame's path is\n    wherever this happens to be installed - a container's site-packages, an\n    editable checkout - and none of those spellings exist in the repository.\n    \"\"\"\n    frames = traceback.extract_tb(error.__traceback__)\n\n    if not frames:\n        return \"an unknown line\"\n\n    innermost = frames[-1]\n\n    return f\"{_as_a_repository_path(innermost.filename)}:{innermost.lineno}\"\n\n\ndef _as_a_repository_path(filename: str) -> str:\n    \"\"\"The file as the repository spells it, if it can be recognised.\n\n    Cut at the package directory and put back under `src/`, which is where this\n    shop keeps its source. Anything unrecognisable is reported whole rather than\n    guessed at - a wrong path in a log is worse than a long one, because it\n    sends a reader to a file that does not exist and looks authoritative doing\n    it.\n    \"\"\"\n    parts = PurePath(filename).parts\n\n    if _PACKAGE not in parts:\n        return filename\n\n    return \"/\".join((\"src\", *parts[parts.index(_PACKAGE):]))\n\n\n_PACKAGE = \"io_shop\"\n",
            "path": "src/io_shop/account_page.py"
          },
          {
            "content": "from __future__ import annotations\n\nfrom collections.abc import Callable\nfrom dataclasses import dataclass\n\n\"\"\"The card Io keeps on file for a shopper, which Io does not keep.\n\nThe account page shows the card a shopper will be charged with, and the shop has\nnever held that number: it lives with the payment provider, and the page asks\nfor it while it renders. That makes the provider part of every account page -\nanother company's service on Io's request path, which is the ordinary shape of a\nshop and the reason an outage somewhere else can become an outage here.\n\nNothing in this module retries or invents a card. When the provider will not\nanswer, that is said plainly - in words naming the host, the path and the status\n- and what the shop does about it is the page's decision, not this module's. The\naccount page degrades: it renders without the card panel and reports these words\nbeside the render. Card details are not worth failing a page for, and a shop\nthat fails every account page because somebody else is down has turned their\nincident into its own.\n\nHow the provider is reached is the caller's to supply. The shop states what it\nasks for and what it does with the answer, and whoever is running it says where\nthat answer comes from.\n\"\"\"\n\n# Who Io banks with. A host rather than a product name, because a host is what\n# appears in a failed request's own words - and those words are what somebody\n# reading the logs at three in the morning has to recognise as not-ours.\nPROVIDER_HOST = \"api.io-pay.example\"\n\n# What is asked for. Spelled out rather than built, so the path in a failure\n# line is the path the shop actually requested.\nCARD_PATH = \"/v1/shoppers/{shopper_id}/card\"\n\n# The only answer that carries a card.\n_OK = 200\n\n\n@dataclass(frozen=True)\nclass StoredCard:\n    \"\"\"A card as the provider describes it: enough to show, never enough to\n    charge with.\n\n    The provider holds the number; what it hands back is what a page may\n    display. A shop that received more than this would be storing card details\n    it went to a provider precisely to avoid storing.\n    \"\"\"\n\n    brand: str\n    last_four: str\n\n\n@dataclass(frozen=True)\nclass ProviderAnswer:\n    \"\"\"One reply from the provider: the status it answered with, and the card if\n    it had one.\n\n    The status is carried rather than interpreted by whoever fetched it, because\n    turning a provider's status into the shop's own failure is this module's\n    job - and a fetcher that raised on its own would put that decision in as\n    many places as there are ways to reach the provider.\n    \"\"\"\n\n    status: int\n    card: StoredCard | None = None\n\n\nclass PaymentProviderFailed(Exception):\n    \"\"\"The provider did not answer with a card.\n\n    Named for what the shop can see. Io has no way to tell a provider that is\n    down from one that is refusing this particular shopper, and inventing the\n    distinction in an exception type would be the shop claiming knowledge of\n    another company's internals.\n\n    Raised so that the caller has to decide what to do about it, and caught by\n    the account page, which renders without the card. The raise is not\n    pointless: it is how the page learns there is no card *and* what to say\n    about it, which a function returning `None` could not tell it.\n    \"\"\"\n\n\n# How the provider is reached, given a shopper. A seam rather than a request,\n# because the shop is rendered many times over to produce a minute of telemetry\n# and a socket per render would be thousands of them per read.\ntype AskTheProvider = Callable[[str], ProviderAnswer]\n\n\ndef card_on_file(shopper_id: str, ask: AskTheProvider) -> StoredCard:\n    \"\"\"The card the provider holds for this shopper.\n\n    Raises `PaymentProviderFailed` for anything that is not a card, and the\n    message is the point of the function: it names the provider's host, the\n    path Io asked for, and the status that came back. A failure that said only\n    \"could not load card\" would be indistinguishable, in a log, from a bug in\n    the shop.\n    \"\"\"\n    answer = ask(shopper_id)\n\n    if answer.status != _OK or answer.card is None:\n        raise PaymentProviderFailed(\n            f\"{PROVIDER_HOST} returned {answer.status} for \"\n            f\"{CARD_PATH.format(shopper_id=shopper_id)}\"\n        )\n\n    return answer.card\n",
            "path": "src/io_shop/payment_provider.py"
          },
          {
            "content": "from __future__ import annotations\n\nfrom pathlib import Path\n\nfrom io_shop import payment_provider, spend_summary\nfrom io_shop.account_page import serve_account_page\nfrom io_shop.accounts import Account, Purchase\nfrom io_shop.payment_provider import AskTheProvider, ProviderAnswer, StoredCard\nfrom io_shop.summary_cache import CacheAnswer, CacheEndpoint, LookUpSummary\n\n\"\"\"The shop's request boundary: what a caller sees when the page fails.\n\nThree things worth pinning: a failure is reported rather than raised - a handler\nthat let it escape would take the worker down - the report keeps the error's own\nwords, which are what a reader diagnoses from, and a payment provider that will\nnot answer costs the page its card panel in words that name the provider,\nwithout costing the shopper the page.\n\"\"\"\n\n\ndef a_provider_holding_a_card() -> AskTheProvider:\n    return lambda dont_care_shopper: ProviderAnswer(\n        status=200, card=StoredCard(brand=\"visa\", last_four=\"4242\")\n    )\n\n\ndef a_provider_that_is_down() -> AskTheProvider:\n    return lambda dont_care_shopper: ProviderAnswer(status=503)\n\n\ndef an_account_idle_this_month(*prices: int) -> Account:\n    return Account(\n        shopper_id=\"shopper-idle-this-month\",\n        purchases=tuple(\n            Purchase(price_cents=price, in_current_month=False) for price in prices\n        ),\n        total_cents=sum(prices),\n        total_this_month_cents=0,\n    )\n\n\ndef an_account_that_never_bought_anything() -> Account:\n    \"\"\"A shopper with no purchase history at all - the lifetime figure's own\n    empty divisor, and the failure this file is built on.\n    \"\"\"\n    return Account(\n        shopper_id=\"shopper-with-no-history\", purchases=(), total_cents=0,\n        total_this_month_cents=0\n    )\n\n\ndef test_a_page_that_renders_carries_the_figure_and_no_failure() -> None:\n    account = an_account_idle_this_month(1000, 3000)\n\n    page = serve_account_page(account,\n                              use_monthly_summary=False,\n                              ask_the_provider=a_provider_holding_a_card())\n\n    assert page.figure_cents == 2000\n    assert page.card_last_four == \"4242\"\n    assert page.failure is None\n\n\ndef test_a_page_that_breaks_is_reported_rather_than_raised() -> None:\n    page = serve_account_page(\n        an_account_that_never_bought_anything(),\n        use_monthly_summary=False,\n        ask_the_provider=a_provider_holding_a_card()\n    )\n\n    assert page.figure_cents is None\n    assert page.failure is not None\n\n\ndef test_a_failure_keeps_the_errors_own_words() -> None:\n    # \"request failed\" describes every incident equally. The type and message\n    # are what put `ZeroDivisionError` in the logs an investigation reads.\n    page = serve_account_page(\n        an_account_that_never_bought_anything(),\n        use_monthly_summary=False,\n        ask_the_provider=a_provider_holding_a_card()\n    )\n\n    assert page.failure is not None\n    assert page.failure.startswith(\"ZeroDivisionError: division by zero\")\n\n\ndef test_a_failure_names_the_line_it_was_raised_on() -> None:\n    # The innermost frame, not the boundary's own: every failure in the shop is\n    # caught in the same place, so the boundary's line describes all of them and\n    # locates none. This is the one that divided by zero.\n    page = serve_account_page(\n        an_account_that_never_bought_anything(),\n        use_monthly_summary=False,\n        ask_the_provider=a_provider_holding_a_card()\n    )\n\n    assert page.failure is not None\n    assert \"at src/io_shop/spend_summary.py:\" in page.failure\n\n\ndef test_the_line_a_failure_names_is_the_one_that_raised_it() -> None:\n    # The number, not just the file. A frame reported off by a few lines sends a\n    # reader to code that is fine and reads as authoritatively as a right one.\n    source = (\n        Path(spend_summary.__file__).read_text(encoding=\"utf-8\").splitlines()\n    )\n    page = serve_account_page(\n        an_account_that_never_bought_anything(),\n        use_monthly_summary=False,\n        ask_the_provider=a_provider_holding_a_card()\n    )\n\n    assert page.failure is not None\n    named = int(page.failure.rsplit(\":\", 1)[1])\n\n    assert \"//\" in source[named - 1]\n\n\ndef test_a_provider_that_will_not_answer_costs_the_card_and_not_the_page() -> None:\n    # The incident, as a case. The card is one panel sourced from another\n    # company; the figure beside it is Io's own work and is still correct. A\n    # page that failed here would put somebody else's outage into Io's error\n    # rate, which is what took every account page down when the provider\n    # started answering 503.\n    page = serve_account_page(an_account_idle_this_month(1000, 3000),\n                              use_monthly_summary=False,\n                              ask_the_provider=a_provider_that_is_down())\n\n    assert page.failure is None\n    assert page.figure_cents == 2000\n    assert page.card_last_four is None\n\n\ndef test_a_provider_failure_is_reported_beside_the_page_not_in_it() -> None:\n    # The host, the status and the raising line, because those are what tell a\n    # reader at three in the morning that the fault is not in this repository -\n    # and they belong in `card_failure`, where they are visible in the logs\n    # without being visible in the error rate.\n    page = serve_account_page(an_account_idle_this_month(1000, 3000),\n                              use_monthly_summary=False,\n                              ask_the_provider=a_provider_that_is_down())\n\n    assert page.failure is None\n    assert page.card_failure is not None\n    assert page.card_failure.startswith(\"PaymentProviderFailed: \")\n    assert payment_provider.PROVIDER_HOST in page.card_failure\n    assert \"503\" in page.card_failure\n    assert \"at src/io_shop/payment_provider.py:\" in page.card_failure\n\n\ndef test_a_provider_that_answers_reports_no_card_failure() -> None:\n    # The other state. Degrading when the provider is down is only right if the\n    # page is unchanged when it is up.\n    page = serve_account_page(an_account_idle_this_month(1000, 3000),\n                              use_monthly_summary=False,\n                              ask_the_provider=a_provider_holding_a_card())\n\n    assert page.card_last_four == \"4242\"\n    assert page.card_failure is None\n\n\ndef a_cache_holding(summary_cents: int) -> LookUpSummary:\n    return lambda dont_care_shopper: CacheAnswer(\n        reached=True, summary_cents=summary_cents\n    )\n\n\ndef a_cache_holding_nothing() -> LookUpSummary:\n    return lambda dont_care_shopper: CacheAnswer(reached=True)\n\n\ndef a_cache_that_cannot_be_reached() -> LookUpSummary:\n    return lambda dont_care_shopper: CacheAnswer(reached=False)\n\n\nSOME_CACHE_ENDPOINT = CacheEndpoint(host=\"cache.io-shop.svc.cluster.local\", port=6379)\n\n\ndef test_a_page_whose_figure_was_cached_says_so() -> None:\n    page = serve_account_page(an_account_idle_this_month(1000, 3000),\n                              use_monthly_summary=False,\n                              ask_the_provider=a_provider_holding_a_card(),\n                              look_up_summary=a_cache_holding(999),\n                              cache_endpoint=SOME_CACHE_ENDPOINT)\n\n    assert page.figure_cents == 999\n    assert page.served_from_cache\n\n\ndef test_a_miss_is_computed_and_the_page_is_still_correct() -> None:\n    account = an_account_idle_this_month(1000, 3000)\n\n    computed = serve_account_page(account,\n                                  use_monthly_summary=False,\n                                  ask_the_provider=a_provider_holding_a_card(),\n                                  look_up_summary=a_cache_holding_nothing(),\n                                  cache_endpoint=SOME_CACHE_ENDPOINT)\n    without_a_cache_at_all = serve_account_page(\n        account,\n        use_monthly_summary=False,\n        ask_the_provider=a_provider_holding_a_card()\n    )\n\n    assert computed.figure_cents == without_a_cache_at_all.figure_cents\n    assert not computed.served_from_cache\n\n\ndef test_a_cache_nobody_can_reach_does_not_fail_the_page() -> None:\n    # The whole scenario rests on this. The fallback is the designed behaviour,\n    # so losing the cache is a slowdown and not an outage - which is exactly\n    # why nobody notices it in the error rate.\n    account = an_account_idle_this_month(1000, 3000)\n\n    page = serve_account_page(account,\n                              use_monthly_summary=False,\n                              ask_the_provider=a_provider_holding_a_card(),\n                              look_up_summary=a_cache_that_cannot_be_reached(),\n                              cache_endpoint=SOME_CACHE_ENDPOINT)\n\n    assert page.failure is None\n    assert page.figure_cents is not None\n    assert not page.served_from_cache\n\n\ndef test_an_unreachable_cache_is_reported_beside_the_failure_not_in_it() -> None:\n    # A page that succeeded while something underneath it was broken. The\n    # distinction is the incident: a reader sees this in the logs without\n    # seeing it in the error rate.\n    page = serve_account_page(an_account_idle_this_month(1000, 3000),\n                              use_monthly_summary=False,\n                              ask_the_provider=a_provider_holding_a_card(),\n                              look_up_summary=a_cache_that_cannot_be_reached(),\n                              cache_endpoint=SOME_CACHE_ENDPOINT)\n\n    assert page.failure is None\n    assert page.cache_failure is not None\n    assert \"6379\" in page.cache_failure\n",
            "path": "tests/io_shop/test_account_page.py"
          },
          {
            "content": "from __future__ import annotations\n\nimport pytest\n\nfrom io_shop.account_page import serve_account_page\nfrom io_shop.accounts import Account, Purchase\nfrom io_shop.payment_provider import AskTheProvider, ProviderAnswer, StoredCard\nfrom io_shop.visits import (\n    forget_every_visit,\n    how_many_shoppers_are_remembered,\n    record_visit,\n    what_they_saw_last_time,\n)\n\n\"\"\"What the account page keeps about the shoppers who have been by.\n\nThe store itself is small and does what it says. What these pin down is the\npart that matters to an incident: it grows with every new shopper and shrinks\nfor no reason at all, which is the shape of the fault the leak scenario is\nabout - and that a new process starts empty, which is why restarting the shop\nis worth anything.\n\"\"\"\n\n\n@pytest.fixture(autouse=True)\ndef a_shop_that_has_just_started() -> None:\n    \"\"\"Every case begins with an empty store.\n\n    The store is module state, so without this each test would inherit\n    whatever the last one left - which is the bug under discussion, and a poor\n    thing to also have in the tests about it.\n    \"\"\"\n    forget_every_visit()\n\n\ndef a_provider_holding_a_card() -> AskTheProvider:\n    \"\"\"The provider answering, which is what it does in almost every case here -\n    these are about what the page remembers, not about who it asks.\"\"\"\n    return lambda dont_care_shopper: ProviderAnswer(\n        status=200, card=StoredCard(brand=\"visa\", last_four=\"4242\")\n    )\n\n\ndef a_provider_that_is_down() -> AskTheProvider:\n    return lambda dont_care_shopper: ProviderAnswer(status=503)\n\n\ndef an_account(shopper_id: str, *prices: int) -> Account:\n    return Account(\n        shopper_id=shopper_id,\n        purchases=tuple(\n            Purchase(price_cents=price, in_current_month=False) for price in prices\n        ),\n        total_cents=sum(prices),\n        total_this_month_cents=0,\n    )\n\n\ndef test_a_shopper_who_has_been_here_before_is_remembered() -> None:\n    record_visit(\"shopper-1\", \"2000\")\n\n    assert what_they_saw_last_time(\"shopper-1\") == \"2000\"\n\n\ndef test_a_shopper_who_has_never_been_here_is_not() -> None:\n    assert what_they_saw_last_time(\"shopper-nobody\") is None\n\n\ndef test_the_newest_visit_is_the_one_kept() -> None:\n    record_visit(\"shopper-1\", \"2000\")\n    record_visit(\"shopper-1\", \"3000\")\n\n    assert what_they_saw_last_time(\"shopper-1\") == \"3000\"\n\n\ndef test_every_new_shopper_adds_to_what_the_process_is_holding() -> None:\n    # The fault, stated plainly: what this holds is a function of how many\n    # different shoppers have been by, and nothing takes any of it away.\n    for shopper in range(50):\n        record_visit(f\"shopper-{shopper}\", \"2000\")\n\n    assert how_many_shoppers_are_remembered() == 50\n\n\ndef test_a_shopper_coming_back_adds_nothing() -> None:\n    # Worth pinning because it is what makes the climb track *traffic* rather\n    # than requests: a shop serving the same hundred shoppers all day holds a\n    # hundred entries, and one serving the internet holds the internet.\n    for _ in range(50):\n        record_visit(\"shopper-1\", \"2000\")\n\n    assert how_many_shoppers_are_remembered() == 1\n\n\ndef test_serving_a_page_records_the_visit() -> None:\n    serve_account_page(an_account(\"shopper-1\", 1000, 3000),\n                       use_monthly_summary=False,\n                       ask_the_provider=a_provider_holding_a_card())\n\n    assert what_they_saw_last_time(\"shopper-1\") == \"2000\"\n\n\ndef test_a_page_missing_its_card_records_the_figure_it_showed() -> None:\n    # A provider outage leaves a page that rendered, so what the shopper was\n    # shown is the figure - not an error string they never saw.\n    serve_account_page(an_account(\"shopper-1\", 1000, 3000),\n                       use_monthly_summary=False,\n                       ask_the_provider=a_provider_that_is_down())\n\n    assert what_they_saw_last_time(\"shopper-1\") == \"2000\"\n\n\ndef test_a_page_that_failed_is_a_visit_too() -> None:\n    # The shopper was here. A store that only grew on success would leave the\n    # shop leaking at a rate that depended on how well it was working, which is\n    # not how retained state behaves.\n    serve_account_page(an_account(\"shopper-1\"),\n                       use_monthly_summary=False,\n                       ask_the_provider=a_provider_holding_a_card())\n\n    remembered = what_they_saw_last_time(\"shopper-1\")\n\n    assert remembered is not None\n    assert remembered.startswith(\"ZeroDivisionError\")\n\n\ndef test_serving_pages_to_many_shoppers_holds_one_entry_each() -> None:\n    for shopper in range(20):\n        serve_account_page(\n            an_account(f\"shopper-{shopper}\", 1000, 3000),\n            use_monthly_summary=False,\n            ask_the_provider=a_provider_holding_a_card()\n        )\n\n    assert how_many_shoppers_are_remembered() == 20\n\n\ndef test_a_restarted_shop_is_holding_nothing() -> None:\n    # What a new process starts with, and the whole of what a restart buys. The\n    # fault is still in the code when it comes back, which is why the climb\n    # begins again - but it begins from here.\n    for shopper in range(20):\n        record_visit(f\"shopper-{shopper}\", \"2000\")\n\n    forget_every_visit()\n\n    assert how_many_shoppers_are_remembered() == 0\n",
            "path": "tests/io_shop/test_visits.py"
          }
        ],
        "summary": "Render the account page without the card panel when the payment provider fails, instead of failing the whole request"
      },
      "name": "submit_fix",
      "type": "tool_use"
    }
  ],
  "diagnostics": null,
  "id": "msg_011CfQTHwe48UV8eLf392nN5",
  "model": "claude-opus-5",
  "role": "assistant",
  "stop_details": null,
  "stop_reason": "tool_use",
  "stop_sequence": null,
  "type": "message",
  "usage": {
    "cache_creation": {
      "ephemeral_1h_input_tokens": 0,
      "ephemeral_5m_input_tokens": 679
    },
    "cache_creation_input_tokens": 679,
    "cache_read_input_tokens": 15126,
    "inference_geo": "global",
    "input_tokens": 2,
    "output_tokens": 26977,
    "output_tokens_details": {
      "thinking_tokens": 1795
    },
    "service_tier": "standard"
  }
}
